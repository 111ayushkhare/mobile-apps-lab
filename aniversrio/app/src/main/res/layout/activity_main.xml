<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_margin="32dp"
    android:orientation="vertical"
    tools:context=".MainActivity">

    <!-- Quotation LinearLayout -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:orientation="horizontal">

        <!-- View for very thin design -->
        <View
            android:layout_width="10dp"
            android:layout_height="48dp"
            android:layout_gravity="center"
            android:layout_weight="0"
            android:background="#31b3eb" />

        <!-- Quotation TextView -->
        <TextView
            android:id="@+id/heading_text_view"
            android:layout_width="0dp"
            android:layout_height="48dp"
            android:layout_gravity="center_vertical"
            android:layout_weight="1"
            android:fontFamily="sans-serif-condensed-medium"
            android:paddingLeft="10dp"
            android:text="@string/quotation"
            android:textColor="@android:color/darker_gray"
            android:textSize="22dp" />

    </LinearLayout>

    <!-- Birthday boy ImageView -->
    <ImageView
        android:id="@+id/image_happy_birthday"
        android:layout_width="wrap_content"
        android:layout_height="0dp"
        android:layout_weight="5"
        android:background="@drawable/image_styling"
        android:elevation="10dp"
        android:padding="3dp"
        android:layout_gravity="center_horizontal"
        android:src="@drawable/happy_birthday" />

    <!-- Brief description of image , TextView -->
    <TextView
        android:id="@+id/description_text_view"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginLeft="10dp"
        android:layout_marginTop="24dp"
        android:layout_marginRight="10dp"
        android:layout_marginBottom="64dp"
        android:layout_weight="3"
        android:background="@drawable/description_styling"
        android:elevation="5dp"
        android:fontFamily="@font/aladin"
        android:padding="16dp"
        android:text="@string/description"
        android:textColor="@android:color/white"
        android:textSize="20dp"
        android:textStyle="italic" />

    <!-- Jump over button -->
    <Button
        android:id="@+id/button"
        android:layout_width="wrap_content"
        android:layout_height="0dp"
        android:layout_gravity="center_horizontal"
        android:layout_marginLeft="48dp"
        android:layout_marginRight="48dp"
        android:layout_marginBottom="16dp"
        android:layout_weight="1"
        android:background="@drawable/button_styling"
        android:elevation="5dp"
        android:fontFamily="sans-serif"
        android:onClick="next"
        android:padding="5dp"
        android:text="@string/jumpNext"
        android:textAllCaps="true"
        android:textColor="#31b3eb"
        android:textSize="20sp"
        android:textStyle="bold" />

</LinearLayout>